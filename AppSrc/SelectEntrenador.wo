// C:\DataFlex Projects\FutbolWebApp\AppSrc\SelectEntrenador.wo
// SelectEntrenador
//

Use cWebView.pkg
Use cWebList.pkg
Use cWebMenuGroup.pkg
Use cWebMenuItem.pkg
Use cWebColumnButton.pkg
Use cWebColumn.pkg

Use cligaDataDictionary.dd
Use cequipoDataDictionary.dd
Use centrenadorDataDictionary.dd

Object oSelectEntrenador is a cWebView
    Set psCaption to "SelectEntrenador"
    Set peWebViewStyle to wvsDrilldown
    Set peViewType to vtSelect
    Set piColumnCount to 12
    Set pbShowCaption to False
    Set piMaxWidth to 1024

    Object oliga_DD is a cligaDataDictionary
    End_Object 

    Object oequipo_DD is a cequipoDataDictionary
        Set DDO_Server To oliga_DD
    End_Object 

    Object oentrenador_DD is a centrenadorDataDictionary
        Set DDO_Server To oequipo_DD
    End_Object 
    
    Set Main_DD to oentrenador_DD //DD Principal es Entrenador
    Set Server  To oentrenador_DD


    Object oList is a cWebList
        Set piColumnSpan to 0
        Set psCSSClass to "MobileList"
        Set pbServerOnRowClick to True
        Set pbFillHeight to True
        Set pbShowHeader to False
        Set piSortColumn to 0

        Object oentrenadorNombre is a cWebColumn
            Entry_Item entrenador.Nombre
            Set psCaption to "entrenador.Nombre"
            Set piWidth to 745
        End_Object 

        Object oentrenadorApellido is a cWebColumn
            Entry_Item entrenador.Apellido
            Set psCaption to "entrenador.Apellido"
            Set piWidth to 745
        End_Object 

        Object oentrenadorNacionalidad is a cWebColumn
            Entry_Item entrenador.Nacionalidad
            Set psCaption to "entrenador.Nacionalidad"
            Set piWidth to 621
        End_Object 
        
        //Este es el objeto que produce la navegación hacía ZoomEntrenador, Boton de i -INVOKINGOBJECT
        Object oDetailButton is a cWebColumnButton
            Set psCaption to "btn"
            Set piWidth to 45
            Set pbFixedWidth to True
            Set pbResizable to False
            Set piListRowSpan to 1
            Set psBtnCssClass to "WebButtonIcon WebIcon_Info"
            Set peAlign to alignRight

            WebRegisterPath ntNavigateForward oZoomEntrenador //Navegación con relación

            Procedure OnClick
                Send NavigatePath
            End_Procedure
            
            //Este es el evento de navegación, InvokingObject. 
            Procedure OnGetNavigateForwardData tWebNavigateData ByRef NavigateData Handle hoToView
                Move True to NavigateData.bReadOnly //es solo en modo lectura.
            End_Procedure

        End_Object 
        
            // Esta navegación pertenece al objetolista, al clicar sobre una fila voy a S.Equipo (pq entrenador es hijo de equipo)
            WebRegisterPath ntNavigateForward oSelectEquipo

        Procedure OnRowClick String sRowId
            tWebNavigateData NavigateData
            Get GetNavigateData to NavigateData

            Case Begin
                Case (NavigateData.eNavigateType = nfFromParent)
                    Send NavigateForward of oSelectEquipo Self //Equipo(padre)
                    Case Break

                Case (NavigateData.eNavigateType = nfFromChild)
                    Send NavigateClose Self
                    Case Break

                Case (NavigateData.eNavigateType = nfFromMain)
                    Send NavigateClose Self
                    Case Break

                Case Else // must be nfUndefined
                    Send NavigateForward of oSelectEquipo Self //Equipo(padre)

            Case End
        End_Procedure

        Procedure OnGetNavigateForwardData tWebNavigateData ByRef NavigateData Handle hoToView
            Move True to NavigateData.bReadOnly
        End_Procedure

    End_Object 

    Object oActionGroup is a cWebMenuGroup

        Object oSearch is a cWebMenuItem
            Set psCSSClass to "WebPromptMenuItem"
            Set psCaption to "Search"

            Procedure OnClick
                Send Search of oList
            End_Procedure

        End_Object 

        Object oNewButton is a cWebMenuItem
            Set psCSSClass to "WebClearMenuItem"
            Set psCaption to "New"

            WebRegisterPath ntNavigateForward oZoomEntrenador 

            Procedure OnClick
                Send NavigatePath
            End_Procedure
            //Este es el evento de navegación de newButton, es decir, es el invokingObject de la invokingView
            Procedure OnGetNavigateForwardData tWebNavigateData ByRef NavigateData Handle hoToView
                Move True to NavigateData.bNewRecord //Guarda el nuevo dato
            End_Procedure

        End_Object 
    End_Object 
      
    Procedure OnNavigateForward tWebNavigateData NavigateData Integer hoInvokingView Integer hoInvokingObject
        WebSet pbRender of oNewButton to True
        WebSet pbRender of oDetailButton to True

        Case Begin
            Case (NavigateData.eNavigateType = nfFromParent)
                // If from parent, this is a constrained drill down
                Case Break

            Case (NavigateData.eNavigateType = nfFromChild)
                // If from child, this is a probably a parent lookup from a Zoom
                Case Break

            Case (NavigateData.eNavigateType = nfFromMain)
                // If from main, this is a probably a main-file lookup from a Zoom
                WebSet pbRender of oDetailButton to False
                Case Break

            Case Else // must be nfUndefined
                // This may be the start of a drilldown query or this may be used for some kind of custom query.

        Case End
        

    End_Procedure
    
   // Este evento no se muestra en pantalla pq ya se está mostrando, no se puede enviar un infobox cuando ya estoy
    Procedure OnShow
        Forward Send OnShow
        Send ShowInfoBox "OnShow de SelectEntrenador"
    End_Procedure
    
//  pq no me sale en evento en la lista de eventos seleccionados y ovviamente tampoco me sale en la web, lo he escrito manualmente. 
    Procedure OneBeforeShow
        Forward Send OneBeforeShow
        Send ShowInfoBox "OnBeforeShow de SelectEntrenador"
    End_Procedure
        
End_Object 
